# CLAUDE.md

## Энэ репо

Хувийн судалгааны тэмдэглэл (`*.md` файлууд) болон `tools/` доторх туслах
хэрэгслүүд. Хариултаа монголоор өг.

## Таск удирдлага — `tools/task/`

Хэрэглэгчийн таскууд `tools/task/task.py` CLI-ээр удирдагдана. Хэрэглэгч
таск, төлөвлөгөө, "юу хийх ёстой вэ", "хийсэн", "дуусгасан", "сануул",
"жагсаалт" гэх мэт зүйл дурдвал **энэ хэрэгслийг ашигла** — өөр газар
жагсаалт үүсгэж болохгүй.

```bash
./tools/task/task ls                 # нээлттэй таскууд
./tools/task/task ls --due overdue   # хугацаа хэтэрсэн
./tools/task/task ls -a              # хаагдсаныг ч оруулаад
./tools/task/task add "Гарчиг" -p high -d маргааш -t ажил -n "тэмдэглэл"
./tools/task/task done 3             # дуусгах (олон ID зэрэг өгч болно)
./tools/task/task start 3            # хийж эхэлсэн
./tools/task/task edit 3 -d +2w --add-tag яаралтай
./tools/task/task rm 3 -y            # устгах
./tools/task/task stats              # статистик
./tools/task/task next               # дараа нь юу хийхийг санал болгох
```

Ач холбогдол: `low` / `med` / `high` / `urgent`.
Хугацаа: `2026-08-20`, `08-20`, `өнөөдөр`, `маргааш`, `+3d`, `2w`, `5`.
Дэд командыг монголоор ч дуудаж болно (`нэм`, `жагсаалт`, `дуусга`, …).

### Ажиллах дүрэм

- **Уншихдаа** `task ls` / `task show <id>` / `task stats`-ыг чөлөөтэй ажиллуул.
- **Устгахын өмнө** (`rm`) хэрэглэгчээс асуу. Нэмэх, дуусгах, засах нь
  хэрэглэгч тодорхой хүссэн үед асуухгүйгээр хийж болно.
- **Өгөгдлийн файлыг гараар бүү засварла** (`~/.tasks.json`). Зөвхөн CLI-аар
  ажилла — тэгвэл ID, timestamp, атомик бичилт зөв хадгалагдана.
- Хэрэглэгч ярианаас олон таск гаргаж өгөхийг хүсвэл `task add`-ыг дараалан
  ажиллуул. Нэг бүрчлэн баталгаажуулах шаардлагагүй.
- Хэрэв хэрэглэгч вэб интерфэйс асаахыг хүсвэл: `./tools/task/task web --open`.
  Энэ нь зөвхөн 127.0.0.1 дээр ажилладаг — гаднаас хандах боломжгүй.

### Тест

Хэрэгслийг өөрчилсөн бол тест ажиллуул:

```bash
python3 -m unittest discover -s tools/task -p 'test_*.py'
```

Дэлгэрэнгүй заавар: `tools/task/README.md`.
